"""Autogenerated OpenBB /fixedincome/government Module."""

### THIS FILE IS AUTO-GENERATED. DO NOT EDIT. ###

#  pylint: disable=R0917,C0103,C0415

from openbb_core.app.static.container import Container
from openbb_core.app.model.obbject import OBBject
import datetime
from typing import Annotated, Union, Optional, Literal
from openbb_core.app.static.utils.decorators import exception_handler, validate

from openbb_core.app.static.utils.filters import filter_inputs


from openbb_core.app.model.field import OpenBBField


class ROUTER_fixedincome_government(Container):
    """/fixedincome/government
    tips_yields
    treasury_rates
    yield_curve
    """

    def __repr__(self) -> str:
        return self.__doc__ or ""

    @exception_handler
    @validate
    def tips_yields(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get current Treasury inflation-protected securities yields.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: fred.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.
maturity : str
    The maturity of the security in years - 5, 10, 20, 30 - defaults to all. Note that the maturity is the tenor of the security, not the time to maturity. (provider: fred)
    Choices for fred: '5', '10', '20', '30'
frequency : str
    Frequency aggregation to convert high frequency data to lower frequency.
    None = No change
    a = Annual
    q = Quarterly
    m = Monthly
    w = Weekly
    d = Daily
    wef = Weekly, Ending Friday
    weth = Weekly, Ending Thursday
    wew = Weekly, Ending Wednesday
    wetu = Weekly, Ending Tuesday
    wem = Weekly, Ending Monday
    wesu = Weekly, Ending Sunday
    wesa = Weekly, Ending Saturday
    bwew = Biweekly, Ending Wednesday
    bwem = Biweekly, Ending Monday
 (provider: fred)
    Choices for fred: 'a', 'q', 'm', 'w', 'd', 'wef', 'weth', 'wew', 'wetu', 'wem', 'wesu', 'wesa'
        'bwew', 'bwem'
aggregation_method : str
    A key that indicates the aggregation method used for frequency aggregation.
    avg = Average
    sum = Sum
    eop = End of Period
 (provider: fred)
    Choices for fred: 'avg', 'sum', 'eop'
transform : str
    Transformation type
    None = No transformation
    chg = Change
    ch1 = Change from Year Ago
    pch = Percent Change
    pc1 = Percent Change from Year Ago
    pca = Compounded Annual Rate of Change
    cch = Continuously Compounded Rate of Change
    cca = Continuously Compounded Annual Rate of Change
 (provider: fred)
    Choices for fred: 'chg', 'ch1', 'pch', 'pc1', 'pca', 'cch', 'cca'

Returns
-------
OBBject
    results : list[TipsYields]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

TipsYields
----------
date : date
    The date of the data.
symbol : str | None
    Symbol representing the entity requested in the data.
due : date | None
    The due date (maturation date) of the security.
name : str | None
    The name of the security.
value : Optional[float]
    The yield value.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.government.tips_yields()
>>> obb.fixedincome.government.tips_yields(maturity='10')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/government/tips_yields",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.government.tips_yields",
                        ('fred',),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def treasury_rates(
        self,
        start_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='Start date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        end_date: Annotated[
            Union[datetime.date, None, str] | None,
            OpenBBField(
                description='End date of the data, in YYYY-MM-DD format.'
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['federal_reserve', 'fmp']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: federal_reserve, fmp.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Government Treasury Rates.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: federal_reserve, fmp.
start_date : date | str | None
    Start date of the data, in YYYY-MM-DD format.
end_date : date | str | None
    End date of the data, in YYYY-MM-DD format.

Returns
-------
OBBject
    results : list[TreasuryRates]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

TreasuryRates
-------------
date : date
    The date of the data.
week_4 : float | None
    4 week Treasury bills rate (secondary market).
month_1 : float | None
    1 month Treasury rate.
month_2 : float | None
    2 month Treasury rate.
month_3 : float | None
    3 month Treasury rate.
month_6 : float | None
    6 month Treasury rate.
year_1 : float | None
    1 year Treasury rate.
year_2 : float | None
    2 year Treasury rate.
year_3 : float | None
    3 year Treasury rate.
year_5 : float | None
    5 year Treasury rate.
year_7 : float | None
    7 year Treasury rate.
year_10 : float | None
    10 year Treasury rate.
year_20 : float | None
    20 year Treasury rate.
year_30 : float | None
    30 year Treasury rate.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.government.treasury_rates()

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/government/treasury_rates",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.government.treasury_rates",
                        ('federal_reserve', 'fmp'),
                    )
                },
                standard_params={
                    "start_date": start_date,
                    "end_date": end_date,
                },
                extra_params=kwargs,
            )
        )

    @exception_handler
    @validate
    def yield_curve(
        self,
        date: Annotated[
            Union[datetime.date, str, None, list[datetime.date | str | None]],
            OpenBBField(
                description=(
                    'A specific date to get data for. By default is the'
                    'current data. Multiple comma separated items'
                    'allowed for provider(s): econdb, federal_reserve,'
                    'fmp, fred.'
                )
            )
        ] = None,
        provider: Annotated[
            Optional[Literal['econdb', 'federal_reserve', 'fmp', 'fred']],
            OpenBBField(
                description=(
                    'The provider to use, by default None. If None, the'
                    'priority list configured in the settings is used.'
                    'Default priority: econdb, federal_reserve, fmp,'
                    'fred.'
                )
            )
        ] = None,
        **kwargs
    ) -> OBBject:
        """Get yield curve data by country and date.

Parameters
----------
provider : str
    The provider to use, by default None. If None, the priority list configured in the settings is used. Default priority: econdb, federal_reserve, fmp, fred.
date : date | str | list[date | str | None] | None
    A specific date to get data for. By default is the current data. Multiple comma separated items allowed for provider(s): econdb, federal_reserve, fmp, fred.
country : str
    The country to get data. New Zealand, Mexico, Singapore, and Thailand have only monthly data. The nearest date to the requested one will be used. Multiple comma separated items allowed. (provider: econdb)
    Choices for econdb: 'australia', 'canada', 'china', 'ecb_instantaneous_forward', 'ecb_par_yield'
        'ecb_spot_rate', 'hong_kong', 'india', 'japan', 'mexico', 'new_zealand'
        'russia', 'saudi_arabia', 'singapore', 'south_africa', 'south_korea', 'taiwan'
        'thailand', 'united_kingdom', 'united_states'
use_cache : bool
    If true, cache the request for four hours. (provider: econdb)
yield_curve_type : str
    Yield curve type. Nominal and Real Rates are available daily, others are monthly. The closest date to the requested date will be returned. (provider: fred)
    Choices for fred: 'nominal', 'real', 'breakeven', 'treasury_minus_fed_funds', 'corporate_spot'
        'corporate_par'

Returns
-------
OBBject
    results : list[YieldCurve]
        Serializable results.
    provider : Optional[str]
        Provider name.
    warnings : Optional[list[Warning_]]
        list of warnings.
    chart : Optional[Chart]
        Chart object.
    extra : dict[str, Any]
        Extra info.

YieldCurve
----------
date : date | None
    The date of the data.
maturity : str
    Maturity length of the security.

Examples
--------
>>> from openbb import obb
>>> obb.fixedincome.government.yield_curve()
>>> obb.fixedincome.government.yield_curve(date='2023-05-01,2024-05-01')
>>> obb.fixedincome.government.yield_curve(date='2023-05-01', country='united_kingdom')
>>> obb.fixedincome.government.yield_curve(yield_curve_type='par_yield')
>>> obb.fixedincome.government.yield_curve(yield_curve_type='real', date='2023-05-01,2024-05-01')

        """  # noqa: E501 # pylint: disable=line-too-long

        return self._run(
            "/fixedincome/government/yield_curve",
            **filter_inputs(
                provider_choices={
                    "provider": self._get_provider(
                        provider,
                        "fixedincome.government.yield_curve",
                        ('econdb', 'federal_reserve', 'fmp', 'fred'),
                    )
                },
                standard_params={
                    "date": date,
                },
                extra_params=kwargs,
                info={'date': {'econdb': {'multiple_items_allowed': True, 'choices': None}, 'federal_reserve': {'multiple_items_allowed': True, 'choices': None}, 'fmp': {'multiple_items_allowed': True, 'choices': None}, 'fred': {'multiple_items_allowed': True, 'choices': None}}, 'country': {'econdb': {'multiple_items_allowed': True, 'choices': ['australia', 'canada', 'china', 'ecb_instantaneous_forward', 'ecb_par_yield', 'ecb_spot_rate', 'hong_kong', 'india', 'japan', 'mexico', 'new_zealand', 'russia', 'saudi_arabia', 'singapore', 'south_africa', 'south_korea', 'taiwan', 'thailand', 'united_kingdom', 'united_states']}}},
            )
        )
